export enum EUserRoles {
  ADMIN = 'admin',
  USER = 'user',
  GUEST = 'guest',
}

export enum ETokenTypes {
  OTP_VERIFY = 'otpVerify',
  OTP_RESET = 'otpReset',
}

export enum EPostScreenTypes {
  CREATE = 'create',
  UPDATE = 'update',
}
